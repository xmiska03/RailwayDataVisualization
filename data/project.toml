# absolute project path
project_path = "/home/zmiskanova/Plocha/BP/bakalarka/data"

# point cloud data
united_pcd_path = "joined/scans.pcd"            # one file with the whole point cloud
divided_pcd_path = "joined/joined_pcd_files"    # point cloud divided into multiple files
divided_pcd_filename_prefix = "pcd"
divided_pcd_files_cnt = 596
divided_pcd_timestamps_path = "joined/joined_pcl_timestamps.txt"

# video
video_path = "../assets/video_long_compatible.mp4"

# vector data
vector_data_path = "vector_data"                # all in one directory, filenames do not matter

# the virtual camera
translations_path = "joined/trans_joined.csv"
rotations_path = "joined/rot_joined.csv"
timestamps_path = "joined/imu_joined_timestamps.csv"

# the train profile
profile_translations_path = "joined/profile"
profile_translations_filename_prefix = "profile_trans"
profile_rotations_path = "joined/profile"
profile_rotations_filename_prefix = "profile_rot"

